window.__FUNCTIONS__.FUNNAME = (params = {}) => {
    // sort function default params
    
    // create a new promise
    let p = new Promise(function(resolve, reject) {
        // Write your function logic here
    });

    // return  the promise
    return p;
}